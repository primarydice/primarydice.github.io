# 制作時のメモ。公開サイトには不要なので、GitHub には上げません。
計画.md
他社AIへの質問文.md
ヒーロー画像のプロンプト.md

# ヒーロー画像の元データを置いた場合の除外先。
# 現在は A(金属) を採用し、assets/hero-a.jpg だけを使っています。
# 元のスクリーンショットは、このフォルダにはもう残っていません。
assets/apps/hero.jpg/

# アプリの動画。そのままWebに載せるには重すぎるため除外しています。
*.mp4
# ただしトップページの帯で流す動画だけは公開します(音声を外して約2.2MBに圧縮したもの)。
!assets/kiban.mp4
# 以前使っていた繰り返し再生の動画。いまは使っていませんが、ファイルは残してあります。
!assets/kiban-loop.mp4

# 画像・動画の元データ(AIで作った素材)。サイトで使うものは縮めて assets/ に置くので、
# 元の大きいファイルは GitHub に上げません。
素材/

# Windows / macOS が勝手に作るファイル
Thumbs.db
desktop.ini
.DS_Store
